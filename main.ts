export { }
let message = 'this is msg';
console.log(message);

//compile this down to plain js by using ts compiler
//by adding import or export statement typescript treat this file as a module instead of script